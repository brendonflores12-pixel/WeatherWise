# WeatherWise

A Next.js + TypeScript weather app using Tailwind CSS.

## Requirements
- Node.js 18+ (or the LTS you prefer) — recommended via nvm
- Recommended package manager: **npm** (see note below if you prefer bun/pnpm/yarn)

## Quick start (npm)
1. Clone:
   ```bash
   git clone https://github.com/brendonflores12-pixel/WeatherWise.git
   cd WeatherWise
